---
title: "Getting_GOSlims"
output: html_document
date: "2024-04-03"
---
```{r}
library(tidyverse)

GOSlims <- read.csv("https://gannet.fish.washington.edu/spartina/project-oyster-oa/Haws/GO-annotation/GO-GOslim.sorted", sep="\t", header = F)

colnames(GOSlims) <- c("GO.IDs", "Pathway", "Process", "Letter")
```

#FDO
```{r}

FDO_sigs_ID_cleaned_separated <- separate_rows(FDO_sigs_ID, GO.IDs, sep = ";")
FDO_sigs_ID_cleaned_separated$GO.IDs <- trimws(FDO_sigs_ID_cleaned_separated$GO.IDs)

FDO_sigs_ID_slim <- left_join(FDO_sigs_ID_cleaned_separated, GOSlims, by = "GO.IDs")

```

#FOA
```{r}
FOA_sigs_ID_cleaned_separated <- separate_rows(FOA_sigs_ID, GO.IDs, sep = ";")
FOA_sigs_ID_cleaned_separated$GO.IDs <- trimws(FOA_sigs_ID_cleaned_separated$GO.IDs)

FOA_sigs_ID_slim <- left_join(FOA_sigs_ID_cleaned_separated, GOSlims, by = "GO.IDs")
```

#FOW
```{r}
FOW_sigs_ID_cleaned_separated <- separate_rows(FOW_sigs_ID, GO.IDs, sep = ";")
FOW_sigs_ID_cleaned_separated$GO.IDs <- trimws(FOW_sigs_ID_cleaned_separated$GO.IDs)

FOW_sigs_ID_slim <- left_join(FOW_sigs_ID_cleaned_separated, GOSlims, by = "GO.IDs")

#Warning: Detected an unexpected many-to-many relationship between `x` and `y`.
```

#GDO
```{r}

GDO_sigs_ID_cleaned_separated <- separate_rows(GDO_sigs_ID, GO.IDs, sep = ";")
GDO_sigs_ID_cleaned_separated$GO.IDs <- trimws(GDO_sigs_ID_cleaned_separated$GO.IDs)

GDO_sigs_ID_slim <- left_join(GDO_sigs_ID_cleaned_separated, GOSlims, by = "GO.IDs")
# Warning: Detected an unexpected many-to-many relationship between `x` and `y`.
```
#GOA
```{r}
GOA_sigs_ID_cleaned_separated <- separate_rows(GOA_sigs_ID, GO.IDs, sep = ";")
GOA_sigs_ID_cleaned_separated$GO.IDs <- trimws(GOA_sigs_ID_cleaned_separated$GO.IDs)

GOA_sigs_ID_slim <- left_join(GOA_sigs_ID_cleaned_separated, GOSlims, by = "GO.IDs")
#Warning: Detected an unexpected many-to-many relationship between `x` and `y`.
```
#GOW
```{r}
GOW_sigs_ID_cleaned_separated <- separate_rows(GOW_sigs_ID, GO.IDs, sep = ";")
GOW_sigs_ID_cleaned_separated$GO.IDs <- trimws(GOW_sigs_ID_cleaned_separated$GO.IDs)

GOW_sigs_ID_slim <- left_join(GOW_sigs_ID_cleaned_separated, GOSlims, by = "GO.IDs")
#Warning: Detected an unexpected many-to-many relationship between `x` and `y`.
```




